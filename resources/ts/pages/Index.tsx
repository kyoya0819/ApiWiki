import React, { FC } from "react";

import Header from "components/Header/Header";

const Index: FC = () => {

    return (
        <div>
            <Header />
            This is Index Page.
        </div>
    );
};

export default Index;
